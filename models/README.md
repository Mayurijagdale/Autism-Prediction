# Models

Place your trained CNN model file here.

- Expected default path: `models/model.onnx`
- Override with environment variable: `MODEL_PATH` (absolute or relative)

Notes:
- Large model files are typically not committed to source control.
- Use a .gitignore in this folder to exclude binaries.
- Supported runtime: ONNX Runtime via `onnxruntime`.
